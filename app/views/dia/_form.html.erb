<%= form_for(@dium) do |f| %>
  <% if @dium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dium.errors.count, "error") %> prohibited this dium from being saved:</h2>

      <ul>
      <% @dium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
