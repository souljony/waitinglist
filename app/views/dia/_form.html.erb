<%= form_for(@dium) do |f| %>
  <% if @dium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dium.errors.count, "error") %> prohibited this dium from being saved:</h2>

      <ul>
      <% @dium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <center><%= f.submit class: "btn btn-large btn-success", value: "Abrir Novo Dia" %>
    <%= link_to 'Voltar', dia_path, class: "btn btn-xs btn-primary" %>
</center>
  </div>
<% end %>
